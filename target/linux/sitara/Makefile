include $(TOPDIR)/rules.mk

ARCH:=aarch64
BOARD:=sitara
BOARDNAME:=TI chip Sitara
SUBTARGETS:=am62
FEATURES:=dt squashfs nand ramdisk gpio

KERNEL_PATCHVER:=6.6

include $(INCLUDE_DIR)/target.mk

$(eval $(call BuildTarget)) 